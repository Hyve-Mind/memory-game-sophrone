import React from "react";
import "../styles/nav.css";

function Nav({ handleCurrentPage, handleLogout }) {
  return (
    <nav className="navbar">
      <div className="nav-title">Memory Card</div>
    </nav>
  );
}

export default Nav;
